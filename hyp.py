ref_point = [100, 100]

front = [[50, 0], [50, 50]]
front = sorted(front, key=lambda x: [x[i] for i in range(len(front[0]))], reverse= True)

Un = [ref_point]

for fi in front: 

    

        
        
        
